{
  "name": "tinyinstaller-panel",
  "version": "1.0.0",
  "description": "Self-hosted OS deployment / VPS re-installation panel (TinyInstaller-style)",
  "private": true,
  "scripts": {
    "start": "node server/index.js"
  },
  "engines": { "node": ">=18" }
}
